# vim: set ft=gitconfig: #
[color "diff"]
    old = red strike
    new = green italic
[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
[user]
    name = Allie Tennyson
    email = agtennyson@gmail.com
[credential]
    helper = osxkeychain
[instaweb]
    local = true
    httpd = webrick
    browser = open
[alias]
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
[merge]
    tool = opendiff
[difftool]
    prompt = false
[mergetool]
    prompt = false
[diff]
    tool = opendiff
[help]
    autoCorrect = -1
[pull]
    ff = only
[push]
    default = current
[log]
    abbrevCommit = true
    decorate = short
[apply]
    ignoreWhitespace = change
[rerere]
    enabled = true
    autoUpdate = true
[am]
    threeWay = true
[color "dif"]
    old = red strike
[color]
    ui = true
